name: Linting

on:
  push:
    branches: [main]
  pull_request:

jobs:

  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: stellar/binaries@v85
        with:
          name: actionlint
          version: 1.7.1
      - run: actionlint
